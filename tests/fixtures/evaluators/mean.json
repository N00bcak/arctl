{
  "subject_command": ["python3", "subject.py", "{input}", "{output}"],
  "prepare_command": ["python3", "evaluator.py", "{request}", "{response}"],
  "calibrate_command": null,
  "score_command": ["python3", "evaluator.py", "{request}", "{response}"],
  "limits": {"timeout_seconds": 60, "max_output_bytes": 1000000},
  "schemas": {
    "public_case": {"type": "object", "additionalProperties": false},
    "subject_result": {
      "type": "object",
      "required": ["score"],
      "properties": {"score": {"type": "number"}},
      "additionalProperties": false
    }
  },
  "public": {
    "statistic": "paired arithmetic-mean score difference",
    "subject_interface": "One numeric result per independent case.",
    "telemetry": {}
  },
  "trial": {
    "meaning": "one independently generated case",
    "dependence": "cases are independent; scores are paired within case",
    "seed_to_case": "the controller seed initializes the case generator",
    "subject_visible_seed": false
  },
  "statistics": {
    "score": "arithmetic mean of paired candidate-minus-champion differences",
    "uncertainty": "one-sided normal standard-error lower bound",
    "positive_effect": "the candidate has a larger mean score"
  },
  "variation": {
    "known": "finite case sampling",
    "mitigations": ["identical paired cases"]
  },
  "suspect_test": {"trigger": null, "reason_codes": []},
  "setup_contract": {
    "environment_adapter": {"entrypoint": "fixture:Environment", "interface": "Python callable"},
    "outcome": {"direction": "higher", "unit": "score", "aggregation": "paired arithmetic mean", "extraction": "subject result score"},
    "trial": {"termination": "case completion", "horizon_unit": "actions"},
    "hard_rules": ["Keep the environment fixed."],
    "runtime_limits": ["60 seconds per process"]
  },
  "calibration": {"supported": false, "policy": null, "ladder": null, "diagnostic": null}
}
